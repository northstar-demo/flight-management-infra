server {
  listen 80;
  server_name _;

  location / {
    root /usr/share/nginx/html;
    try_files /index.html =200;
  }

  location = /flight/health {
    add_header Content-Type text/plain;
    return 200 "flight-ok\n";
  }
}
